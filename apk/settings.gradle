pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        //flatDir {
        // dirs 'app/aars'
        //}
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //flatDir {
        //dirs 'app/aars'
        // }
    }
}
rootProject.name = "MyTouch"
include ':app'
